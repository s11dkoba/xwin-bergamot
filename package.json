{
  "name": "xwin-bergamot",
  "version": "0.1.0",
  "private": true,
  "description": "XWIN corporate site (static).",
  "scripts": {
    "dev": "npx --yes serve . -l 3200",
    "deploy": "npx --yes vercel@latest deploy --prod --yes"
  }
}
